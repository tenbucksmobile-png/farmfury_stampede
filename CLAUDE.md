# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.

# Farm Fury: Stampede

Side-scrolling platformer in the Farm Fury universe. Players pick one of eight farm animals and fight through hand-designed levels across six territories the Harvest Robots have occupied — running, jumping, and using each character's signature ability to defeat robots and reach the end of the level. Every level is completable by any unlocked character, but each also hides secrets that only one specific character's ability can reach. The character-before-level choice (not mid-level swapping, like Rush or Arcade) is the central differentiator.

Full design spec: `FarmFury_Stampede_GDD.md`

## Status

Phases 1-4 done (foundation; controller; prefab-based levels + Harvester/Drone; 8 characters, abilities, gated secrets, unlock ladder). **Phase 5a (robot variety, the reusable boss pattern, the real progression UI, and lives) is built and machine-verified but not yet hand-playtested.** A scripted headless run drove the robots, the boss, World/Level Select, HUD, lives and pause through the real UI buttons. Feel and difficulty (boss stagger/speed, Scout sight range, Chaser speed, 3 lives) need a human pass. **Meadow Ruins environment art is now wired in too** (parallax background, ground/platform/breakable tiles, checkpoint/goal/crop/chamber-backdrop art) - see "Real environment art" below - verified the same way (headless `RunSetup`, no compile errors, scene/prefab wiring spot-checked) but likewise not yet seen running in Play mode.

**Menu art + 12-level worlds (2026-09-25, not yet seen in Play mode):** World Select, Level Select, Character Select and Results were rebuilt around dropped-in art and the user's mockups (see "Menu art" under UI below), with Level/Character Select kept inside the device safe area. Meadow Ruins grew to 11 levels + boss (GDD v1.2: 12 per world, 72 total); levels 9-11 pass `LevelBuilder` validation in a headless Run Setup but need a hand playtest for feel and difficulty. Layouts were checked only as composite previews from the layout numbers, not as screenshots from Unity.

**Level 1 playtest pass (in progress, hand-tested by the user in the editor):** grass-topped ground surface, 1.5-unit characters/robots, double jump, a zoomed-out camera that keeps the floor in view, character-select cards, corn-only pellets, rock/haybale obstacles, a barrel pyramid, a stone-slab secret ledge and three+one Scouts in Level 1. The last few rounds (1.5x obstacle/barrel sizes, 1.2 cob/maize pellets, crop spacing, pellet rest height, stone ledge, the invulnerability-flash fix, the zoomed-out floor-aware camera, barn/windmill background scenery) were written while the editor was open; a headless **Run Setup** on 2026-09-24 (with the Level 1 farm backdrop) compiled them cleanly and rebaked every level prefab, but they still need a hand playtest. Unused dropped-in art: `Plank_Diagonal.png`, `DistantBarn.png`, `RarePellets_maize.png` and the other non-kernel, non-cob crop art.

Phase 5a architecture:
- **Robots** (`Scripts/Robots`): `RobotController` base (a contact is a stomp/ability hit via overridable `TakeHit()`; bosses override it and `CanHurtPlayer`). `GroundPatrolRobot` holds the patrol/wall/ledge logic; `HarvesterRobot` is it as-is, `ScoutRobot` (speed 3.8; turns to face a player who is behind it within sight range with line of sight), `ChaserRobot` (fixed 6.4 u/s vs the player's 8; wakes inside its aggro range = marker patrolDistance; stops at walls/ledges; stompable). `BarrierUnit` (1x3 solid on the Ground layer, refuses every hit, only Billy's Charge Break via `IChargeBreakable`; on open ground it can be jumped over, so use it only in capped passages such as a Chamber). `RobotType.Commander` was appended to the enum.
- **Boss pattern** (`CommanderBoss`, read the comment block at the top): boss level = normal level prefab with `Boss()` (no goal); 3 hits, 1.5s stagger between hits (harmless, un-hittable), speeds up per hit, reinforcement waves via `RobotSpawnPoint.wave` spawned by `LevelLoader.SpawnWave(n)` on the nth hit; defeat -> `GameManager.CompleteLevel()`. Worlds 2-6 duplicate the prefab/RobotData and author a new arena.
- **Double jump**: every character gets `airJumps` (1) extra mid-air jumps at full jump velocity (variable height, reset on landing; `CharacterController2D`). `LevelBuilder`'s reach validation still models only the single base jump, so it can't catch a secret that a double jump now reaches - Level 1's Cluck ledge was lowered to 4 units so the double jump reaches it comfortably (a single jump doesn't).
- **Camera** (`CameraFollow2D`, orthographic size 7.5 set by setup): X follow with look-ahead; Y dead zone + offset, capped by `ClampToFloor` so the first *tilemap* surface under the player (ground/platform/ledge, not obstacle colliders) stays `floorMargin` (2) above the bottom edge - standing on barrels/rocks doesn't lift the ground out of view.
- **Lives**: 3 per attempt (`GameManager.LivesPerAttempt`). Every death (pit, robot, drowning) costs one and respawns at the checkpoint; the third calls `EndLevel(false)` -> failure panel -> auto-return to Level Select after 2.5s. `StartLevel` always resets to 3.
- **Progression rules** (`SaveManager`): a world unlocks when the previous world's boss level is cleared (`SetWorldBossCleared` is the test hook); a level unlocks when the previous level in its world is completed; the boss unlocks after all regular levels. Stars (`StarCalculator`): 1 completed, +1 for >=75% of normal crops, +1 for finding the level's gated secret (or finishing without dying if it has none); time is not scored yet. "Secret found" = a secret-cluster crop was collected (`CropPickup.isSecretCluster`, persisted), which drives the Level Select "?" icon.
- **UI** (`Scripts/UI`, uGUI built in code by `UIKit`): `GameFlow` owns navigation (Landing -> World Select -> Level Select -> Character Select -> HUD/Pause/Results) driven by `GameManager.StateChanged`; menus hide the player and unload the level. Screens: `LandingScreen` (the first screen, `GameState.MainMenu`, from the landing mockup: `Btn_play.png` bottom-left -> World Select, `Exit.png` -> quit (stops Play mode in the editor), `Btn_settings.png` -> `ShopSettingsScreen`, a placeholder with Shop/Settings "coming soon" panels and `Btn_back`; `Btn_home.png` top-left of World Select's safe area (and Esc) goes back to Landing. The art is one finished poster, `Sprites/UI/FF_StampedePoster.png` (`MenuArt.landingPoster`; logo, sign and Cluck painted in), cover-cropped mostly off the bottom on phones wider than 16:9; the buttons sit in the safe area on top), `WorldSelectScreen`, `LevelSelectScreen`, `CharacterSelectScreen`, `HudScreen`, `PauseScreen` (Play/Settings stub/Restart Level/Quit), `ResultsScreen`. Character Select shows each character's framed card (`CharacterData.selectCard`, from `Sprites/UI/<Name>_<Animal>.png` via `StampedeUIArt.CharacterCardFiles`; name baked into the art) with no caption, under the `NewCharacter.png` header banner (`MenuArt.characterSelectBanner`, replaces the title text); locked cards are dimmed by the Button's disabled colour. Its backdrop is the world's Level Select art cropped to the bottom 62% (a `RawImage` uvRect) so the baked-in world title doesn't show around the banner. `Btn_back.png` sits in the top-left of the safe area on both Level Select and Character Select. Characters without a card fall back to the coloured panel + portrait. **Menu art** (not yet seen in Play mode): per-world `WorldData.selectCardArt` (`Sprites/Environment/WS_<World>.png`, World Select cards, name baked in; each card is only its art and the whole card is the tap target into Level Select, locked worlds greyed, on the sunset-farm backdrop `Sprites/Environment/Canvas.png` (`MenuArt.worldSelectBackground`), under a `WorldUnlocked.png` header banner - no status text) and `levelSelectBackground` (`<World>.png`, name along the top), mapped in `StampedeUIArt.WorldArtNames` (files say `SkyIsland`/`Mothership`). Shared art is a `MenuArt` block on `GameFlow`: Level Select follows the mockup: `LevelTile_Locked` padlock when locked, `LevelTile_question` for the next level to play, `LevelWin_1`/`LevelComplete_2`/`_3` Cluck boards by stars once completed, the boss slot is the padlock until every level is done, then `Boss_Shield`, then its star board once beaten; no numbers or secret icons. Every tile's art is drawn at the same visible height (`PlaqueScale`/`BoardScale`/`ShieldScale`, measured from how much of its 500px frame the art fills; the boards are also re-centred). The backdrop is top-anchored (overflow cropped off the bottom) so the baked-in world title stays whole. The grid is 6 wide and sized for 12 slots (11 levels + boss) between the backdrop's title band (top 37%) and the bottom of the device safe area (`SafeAreaFitter`), re-laid out when the safe area changes; a short last row is centred; each tile is an invisible tile-sized hit area with the art as a child, so transparent margins never steal a neighbour's tap; a world with no backdrop keeps the plain card grid. Results shows the star board, plus `NewCharacter` / `WorldUnlocked` signs (`LevelRunState.worldUnlocked` = first clear of a non-final boss). The round Level Select back button is `Btn_back.png`; `SelectLevelSign.png` is imported but unused (not in the mockup). Esc = pause/back. **F1 toggles `DebugPanel`** (complete a world's levels, flip boss cleared, unlock all worlds, level count cheats, reset save).
- **Real character art** lives in `Assets/_Project/Sprites/Characters/` (never in `Sprites/Placeholder/`, which setup regenerates). Setup imports the frames listed in `CharacterArtFiles` (StampedePhase5aSetup) at `ArtPixelsPerUnit` (500px frame / `ActorVisualHeight` 1.5 = 1.5 units tall, feet-pivoted; the Visual child sits on the collider's bottom edge, colliders unchanged), builds a `CharacterSpriteSet` asset (idle/run/jump per direction + defeat) and assigns it to the character's `CharacterData.spriteSet`; `CharacterSpriteAnimator` swaps frames by controller state. Directional art is swapped, not flipped (`HasDirectionalArt` stops the flip); characters without a set keep their placeholder and flip. Only Cluck has art so far. To add a character: drop the frames, add its file list to `CharacterArtFiles`, re-run setup. Frames with "jump" in the filename import at `JumpFrameScale` (1.25x) because the spread-wing pose makes the body look smaller. Deaths now hold the defeat pose ~0.6s at the spot (`LevelLoader.RespawnPlayer`, guarded by `IsRespawning` so repeat contacts cost one life).
- **Real robot art** lives in `Assets/_Project/Sprites/Robots/` (imported by `ImportRobotArt` in setup, same size and feet pivot as characters; the Drone stays centre-pivoted). `RobotController` has optional `spriteRight` / `spriteLeft` / `defeatSprite`; `SetFacing(bool)` swaps sprites when both sides exist, otherwise flips (Drone and Commander have a single frame). Setup assigns them per prefab via `RobotArt(right, left, defeat)`: Harvester = `Robot_Harvest_*`, Scout = `ScoutRobot_*`, Chaser = `DriftRobot_*`, Drone = `Drone.png`, Commander = `Commander_Alert` / `Commander_Defeated`; generic `Robot_defeated` for the ground robots. Not wired yet: `Robot_left.png` (unassigned), the two `*_explode` frames, Barrier Unit art (still placeholder). Not yet verified in Unity.
- **Real environment art** (Meadow Ruins only so far) spans three editor tools, all called from `StampedePhase5aSetup.RunSetup` so a normal re-run keeps everything in sync:
  - **Parallax background** (`StampedeEnvironmentArt.cs`, `Assets/_Project/Sprites/Environment/`): `ParallaxLayer`/`ParallaxBackground` (`Scripts/LevelSystem/`) hold 3 layers (`Layer1/2/3.png` - far sky/hills, mid windmill/village, near flower foreground). Each layer is stretched to exactly cover a level's camera span (`LevelPrefabRoot.cameraMinX/cameraMaxX`) and scrolls at its own depth factor (0.08/0.35/0.65); `LevelLoader` reconfigures it on every `LoadLevel`. `DistantBarn.png` sits unused in the same folder - its softer painterly style doesn't match Layer1-3's flat-cartoon look; drop it into the `Layers` array in `StampedeEnvironmentArt` once it's regenerated to match.
  - **Ground/platform tiles are procedural, not AI-generated** (`StampedeProceduralTiles.cs`): two rounds of AI art (Kling, then Leonardo) only ever produced isometric block icons despite explicit "side elevation, not isometric" prompts, so `GroundTile`/`PlatformTile` are generated directly - mottled dirt-and-stone fill for the buried ground (no grass cap) and wood-plank art for `PlatformTile` (floating platforms are always exactly one tile thick). The grass-topped surface is `Sprites/Environment/FloorTile.png`, a strip of 3 blocks sliced into `FloorTile_0..2` (`StampedeEnvironmentArt`) and turned into `GroundSurfaceTile_0..2`; `LevelBuilder.PaintSurface` swaps every ground cell with nothing above it for one (variant by column). The dirt body is stretched to one cell via the Tile transform and the grass overhangs the cell above (Grid colliders, so visual only).
  - **Gameplay/UI art** (`StampedeUIArt.cs`, `Assets/_Project/Sprites/UI/`): `CropPickup` picks a random sprite (0.6 units, `CropTargetWorldSize`, except `CornCob`/`RarePellets_maize` at 1.2 via `CropSizeOverrides`, pivoted so their bottom edge matches; resting 0.9 above its surface, `LevelBuilder.CropRestHeight`, to clear the grass) from a corn-only pool (`CornCob`, `CornKernel`; secret cluster = `RarePellets_maize`), the other crop art stays unused in `Sprites/UI`; corn rows are doubled up along the row (`LevelBuilder.CropRow` places at half the authored step, never closer than `MinCropSpacing` 1.25 - the widest icon, the cob, is ~1.09); `RemoveOverlappingCrops` runs just before the markers are written and drops any crop that would overlap one already kept (secret crops kept first), which thins the 1-apart hand-placed groups, arcs and secret rows; `Checkpoint` swaps real idle/activated signpost art (`CheckpointFlag.png` / `_pass.png`) instead of tinting a square; `Goal` uses `LevelComplete.png` (a checkered finish flag, despite the filename); `LevelBuilder` fills every `Chamber()` interior (Level 3 and Level 5's secrets) with `BarrierChamberWall.png`; the `BreakableFloor` tile uses the first FloorTile variant tinted orange-brown (`BreakableFloorTint`) so Ground Pound spots read differently from the surface around them. Checkpoint/goal art is feet-pivoted via `StampedeUIArt.SetPivot` (setting `importer.spritePivot` alone is ignored unless alignment is Custom - that is what buried the flags). **Obstacles**: `Rock.png` / `Haybail.png` (`StampedeUIArt.ObstacleFiles`, pivoted where the art meets the ground) are solid 2.1x1.95 Ground-layer blocks (art ~2.1-2.25 tall) baked into each non-boss level prefab by `LevelBuilder.PlaceObstacles` - `ObstaclesPerLevel` (2) per level, art and spot chosen at random with a seed from the level id (stable across re-runs), only on flat main-path ground clear of start/goal/checkpoints/robot patrols/breakables/water/chambers/platforms/secrets; corn under one is lifted onto it. **Background scenery** (`LevelBuilder.PlaceScenery`, no collider, sortingOrder -5 so the grass covers its base, tinted slightly dim): a `DamagedBarn.png` (~4.2 tall) behind each haybale obstacle and a `Windmill.png` (~5.5 tall) behind each barrel pyramid, offset to a seeded-random side; skipped if it would overhang a gap/step or overlap other scenery. **Stone ledges**: every `SecretLedge()` keeps its Platforms-tilemap collision cells but paints them with `InvisibleTile` (no sprite, Grid collider) and draws `Stone_Block.png` slabs over it (`LevelBuilder.AddStoneSlabs`: whole slabs, stretched slightly to span the ledge exactly). `Stone_Square.png` is unused. **Barrel pyramids**: `LevelBuilder.BarrelPyramid(x)` stacks three `Wooden Barrel.png` barrels (1.65x2.25 solid each, 2 base + 1 on top = 4.5 high, climbed in two 2.25 steps since each base barrel sticks out past the top one), clears ground corn under it and puts one corn on top; random obstacles keep 5 units away. Level 1 has one at x=74 with a Scout on the landing side (x=80). **World scale** (`StampedeUIArt.UnitsPerMetre` = 0.65): every world prop is imported from a real-world height in metres (`ObstacleFiles`, `SceneryFiles`, `BarrelMetres`), with pixels-per-unit computed from the art's opaque pixel height, so nothing is out of proportion (barrel and hay bale both 1.5 u - the bale is sized by its body, not its straw skirt, so stacked bales touch - fence 0.8 u, corn 1.7 u, cart 2.2 u, water wheel 3.3 u, barn 5.9 u, silo/windmill 7.8 u, oak 8.5 u). To add a prop: add its file, pivot and metres to the table. Only actors (1.5 u, cartoon), pickups, stone blocks and the distant biplane are outside it. `BarrelPyramid` is 3-2-1 barrels (4.5 high, 1.5 steps, art drawn 12% big so the lids tuck), `HayStack` is 2+1 bales (3 high), and a random haybale obstacle in other levels is one 1.9x1.5 bale. **Level 1 layout** (`MeadowRuinsLevels.Level1`, `ManualScenery()` = no random obstacles and no automatic barn/windmill): the farmstead appears **once** (water wheel + windmill + cart, silo behind the barn, fenced corn with the oak, then the barrel pyramid at 89) and every landmark only once; the rest is fenced corn fields. Mockup 1 section after the gap: `HayStack`, `StoneBlocks(x,len,top)` bonus platforms (square `Stone_Square.png` per tile; validated against the double jump via `MaxBonusRise` 5, so keep them >5 units from a gated secret) with kernels and a `BonusCoin`, two Scouts, the gnarled tree; a second stone-block run near the goal. **Farm backdrop** (`LevelBuilder.PlaceFarmBackdrop`, authored with `CornField`, `Fence`, `Wildflowers`, `Backdrop(FarmProp.*, x)`, `Biplane(height)`): visual only, lifted `BackdropLift` 0.3 so the grass overhang doesn't bury it; sorting -9 biplane (`SkyDrifter`), -8 silo/oak/gnarled/water wheel, -7 barn/windmill, -6/-5 corn rows, -4 cart/fence, -3 flowers (only a bunch at each end of every `Fence` run). **Pickups:** the normal crop pool is now only `CornKernel.png` at 0.9 units (the cob pickup was camouflaged against the backdrop corn); secret-cluster crops use `CornCob.png` (1.2) instead of the maize pellet; `CropSpawnPoint.visualOverride` gives one marker its own art (the coin, `Collectable Coin.png`, which counts as a normal crop until Phase 6's coin economy). 
- **Setup**: menu **Farm Fury Stampede -> Phase 5a -> Run Setup** regenerates everything (supersedes the Phase 3/4 setup). Levels: Meadow Ruins 1-11 + `MeadowRuins_Boss` (12 per world; 9 = Bessie floor, 10 = Gerald/Woolly chasm island, 11 = Billy chamber capstone); Chaser in level 4, Scouts in 6 and 8, level 5's secret is a Barrier-sealed chamber (Billy).

Headless testing tips: `Time.captureFramerate = 50` gives deterministic fixed-step play mode under `-batchmode -nographics`; set `InputSystem.settings.backgroundBehavior = IgnoreFocus` and `editorInputBehaviorInPlayMode = AllDeviceInputAlwaysGoesToGameView` or injected keyboard input is ignored. UI buttons can be driven with `button.onClick.Invoke()`. `Tilemap.GetUsedTilesCount()` counts tile *types*, not tiles. `Object.GetInstanceID()` is obsolete in Unity 6.5. A project reimport can add minutes before play mode starts.

Layers `Ground` (6) and `Player` (7) exist. `com.unity.2d.tilemap.editor` was removed from the manifest (merged into `com.unity.2d.tilemap` in Unity 6.5). Open the project at the repo root, not the nested `FarmFury_Stampede/` folder Unity's GitHub flow can create.

## Stack

- Unity 6.5, 2D template, Universal Render Pipeline (URP)
- C#, single-scene architecture (`Game.unity`), data-driven via ScriptableObjects
- Packages: 2D Sprite, 2D Tilemap Editor (the one package difference from Rush — Stampede is tile-based level geometry, not a scrolling chunk system), TextMeshPro, Input System
- Backend (planned, not yet built): Supabase, matching Arcade's proven `UnityIAPServices`/`StoreController` + LevelPlay approach rather than Rush's still-unverified backend assumptions (see GDD Section 12)
- Art: Kling AI + Photopea cleanup; Audio: Suno AI, Pixabay, Freesound

## Related Farm Fury projects

- `Farm Fury` (main game) — defensive combat, shelved
- `Farm Fury: Arcade` — Pac-Man-style stealth crop reclamation, standalone repo at `C:\Users\Personel\Desktop\FarmFury_Arcade`, near-launch
- `Farm Fury: Rush` — endless runner, standalone repo at `C:\Users\Personel\Desktop\FarmFury_Rush`, Phase 2 of 6 complete
- `Farm Fury: Stampede` (this repo) — side-scrolling platformer

All active titles share a Supabase backend (planned) for cross-game player identity, cross-promotion, and shared cosmetic unlocks. Keep data schema compatible with the others when touching save/cloud-sync code.

## Build order — six phases

Each phase is its own Claude Code session, only started once the previous phase is verified working in the Unity editor. Do not implement a later phase's systems while working an earlier one. This mirrors the phase structure that worked for Rush.

1. **Project foundation** — folder structure, core singletons (`GameManager`, `DataManager`, `SaveManager`, `AudioManager`), ScriptableObject definitions (`CharacterData`, `RobotData`, `LevelData`, `WorldData`), enums, empty `Game.unity` scene, placeholder data. No gameplay. Built.
2. **Core movement & controls** — Cluck only. Kinematic `Rigidbody2D` + `MovePosition` movement, coyote time, jump buffering, one hand-built test level in Meadow Ruins. Highest-risk phase — don't proceed until jump/movement genuinely feels good in playtesting, same lesson as Rush's Phase 2. Built and playtested.
3. **Level system & World 1** — Prefab-based level loading from `LevelData`, the Meadow Ruins levels (8 then; 11 + boss since GDD v1.2), Harvester and Drone robot AI, checkpoints, crop collection. Built; hand-playtest pending (see Status).
4. **Characters & abilities** — remaining 7 characters and all 8 unique abilities, character-select-before-level UI, ability-gated secrets, the 5/10/15/20/30/40 unlock ladder. Built; hand-playtest pending (see Status).
5. **Remaining worlds, bosses & progression UI** — Worlds 2–6, per-world bosses, Robot Overlord finale, World Select/Level Select screens, HUD, pause. Split into sub-phases: **5a done** (robots, boss pattern, UI, lives; hand-playtest pending); 5b Frozen Tundra, 5c Watermill Village, 5d Sky Islands, 5e Sunken City, 5f Robot Mothership + Overlord.
6. **Monetisation & polish** — Unity IAP (Arcade's proven product catalog, adapted), LevelPlay ad mediation, Firebase Analytics, Supabase cloud save, cross-promo, tutorial, launch polish.

## Key architectural facts (from the GDD)

- **8 characters**: Cluck (Flutter Jump, starter), Bessie (Ground Pound, starter), Percy (Roll Dash, unlock @5 levels), Woolly (Cloud Step, @10), Ducky (Skip Dash, @15), Horace (Rear Vault & Horseshoe Throw, @20), Gerald (Puff Glide, @30), Billy (Charge Break, @40).
- **Design rule**: every main path through every level must be completable by *any* unlocked character — abilities gate secrets and bonus areas, never the critical path.
- **5 robot types**: Harvester (ground patrol), Scout (fast patrol), Drone (flying, needs a jump-into or Horace's horseshoe), Barrier Unit (stationary, only Billy's Charge Break clears it), Chaser (pursues at a fixed sub-player speed).
- **6 worlds**, reused from the shelved original Farm Fury's world designs, reframed as platformer territories: Meadow Ruins (tutorial), Frozen Tundra (reduced ice traction), Watermill Village (fire-spread hazards), Sky Islands (wind gusts), Sunken City (reduced-gravity underwater), Robot Mothership (zero-G, Robot Overlord finale). 12 levels per world (11 + the boss as the 12th), 72 total for v1 launch (raised from 46 in GDD v1.2).
- **Controller genuinely differs from Rush's** despite the shared engine: Rush keeps the character fixed in X while the world scrolls past it; Stampede needs a conventional platformer setup where the *character* traverses a Tilemap-space level with a following camera. The Physics2D lesson from Rush still applies — anything needing reliable collision must use a kinematic `Rigidbody2D` moved via `MovePosition`, never `transform.Translate`.
- **Level authoring**: Unity Tilemap + Composite Collider2D for geometry, with level content still defined as data (ScriptableObject descriptors referencing tilemap layouts), matching the franchise's data-driven convention.
- **Ability model**: per-level-use limit, not a real-time cooldown (Arcade/Rush use timer-based cooldowns; a level-based game fits a use-limit better).

## Working with this repo

- Standalone repo (like Rush and Arcade), separate from the shared home-directory repo the rest of this machine's projects live in.
- Follow the exact folder structure specified in the Phase 1 prompt (`Assets/_Project/Scripts/{Core,Data,Data/Enums,Movement,Characters,Robots,LevelSystem,UI,Utilities}`, `Assets/_Project/ScriptableObjects/{Characters,Robots,Levels,Worlds}`, etc.) so it matches franchise conventions for future Claude Code sessions working across all four repos.
- Namespace convention: `FarmFuryStampede.<Area>` (`Core`, `Data`, `Utilities`, ...), matching the folder it lives in.
- Don't link this repo to GitHub via Unity's own project-creation/GitHub-linking flow — it will silently create a *new* default template project and a *differently-named* GitHub repo instead of using the existing one. Manage git for this repo the same way as Rush and Arcade: plain `git` from the command line, this repo's own `.git`, remote `origin` pointed at `github.com/tenbucksmobile-png/farmfury_stampede`.
- `pandas_ta`/freqtrade conventions from other repos on this machine do not apply here — this is a Unity/C# project, unrelated stack.
